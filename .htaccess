RewriteEngine On

# Block access to /rate_limit and /app folders
RewriteRule ^(limiter_data|app)(/.*)?$ - [F,L]

# Continue rewriting everything else to index.php
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]